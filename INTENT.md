# BBVS Intent

本文件记录产品意图与取舍，不定义可测试的强制行为。强制行为见 `SPEC.md`，当前实现见 `IMPL.md`。

## 核心意图

BBVS 要把视频变成可独立阅读、证据可追溯的结构化笔记，而不是 ASR 的缩写版复述。

系统应固化低歧义、可验证的工程工作：素材获取、字幕选择、ASR 回退、OCR、候选帧生成、缓存、
结构化产物和质量检查。章节边界、教学主线、信息重要性、图文对应与叙事重组应由语言/视觉模型
基于证据判断，不由固定时间窗口代替。

## 当前优先级

1. 高效使用平台人工字幕；只有缺失或质量不足时才运行 ASR。
2. 用高召回内容映射保留论点、机制、例子、限制、公式/代码画面需求。
3. 先形成全局大纲，再写章节；章节服务于概念依赖，而不是固定时长。
4. 用语义需求定位时间区间，再机械抽帧和视觉筛选；候选预算按需求分配，不设视频级小常数上限。
5. 公式现阶段以召回原始 PPT 并和正文配对为目标，不要求转录为 LaTeX。
6. 通过独立 coverage/grounding/coherence review 发现漏召回与失真。

## 非核心能力

Embedding、Reranker 和 QA 是可选检索能力，不应决定笔记的证据切分、章节划分或写作流程。
HTML、PDF、LaTeX、Typst 都是 renderer 选择，不是理解质量的核心。

## 模型使用原则

- 传统工具和小模型负责确定性处理与候选生成。
- 便宜 LLM 可承担术语发现和高召回内容抽取。
- 强 LLM 的预算优先用于全局大纲、章节重组和最终审查。
- VLM 用于视觉内容与完整性的最后一公里判断，不用于无差别扫描所有视频帧。

